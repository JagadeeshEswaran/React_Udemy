// import PropTypes from "prop-types";
// import { Book, EventsExample } from "./child";

// import bookList from "../../data/BestBooks/booklist.json";

const PopularBooks = () => {
	// const getBook = (findId) => {
	// 	for (let book of bookList) {
	// 		if (book.id === findId) {
	// 			console.log(`${book.id} || ${book.title}`);
	// 		}
	// 	}
	// };
	// PopularBooks.propTypes = {
	// 	bookData: PropTypes.object.isRequired,
	// };
	// return (
	// 	<div>
	// 		<div className="bookPage">
	// 			{/* <EventsExample /> */}
	// 			<h1>Best Seller in Books</h1>
	// 		</div>
	// 		<div className="booklist">
	// 			{bookList.map((book) =>
	// 				// <Book key={book.id} bookData={book} parentMethod={getBook} />
	// 				console.log(book)
	// 			)}
	// 		</div>
	// 	</div>
	// );
};

export default PopularBooks;
